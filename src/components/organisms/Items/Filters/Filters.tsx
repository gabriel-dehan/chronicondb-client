import React, { FunctionComponent } from 'react';

import './Filters.scss';

const Filters: FunctionComponent = () => {
  return (
    <div className="o-filters">Filters</div>
  );
};

export default Filters;